version: "3.8"

services:
  api-site:
    image: zzpowertech/api-site:3.2
    container_name: api-site
    pull_policy: always
    ports:
      - "3333:3333"
    env_file:
      - .env
    restart: unless-stopped
